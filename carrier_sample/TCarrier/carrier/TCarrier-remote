#!/usr/bin/env python
#
# Autogenerated by Thrift Compiler (0.10.0)
#
# DO NOT EDIT UNLESS YOU ARE SURE THAT YOU KNOW WHAT YOU ARE DOING
#
#  options string: py
#

import sys
import pprint
if sys.version_info[0] > 2:
    from urllib.parse import urlparse
else:
    from urlparse import urlparse
from thrift.transport import TTransport, TSocket, TSSLSocket, THttpClient
from thrift.protocol.TBinaryProtocol import TBinaryProtocol

from carrier import TCarrier
from carrier.ttypes import *

if len(sys.argv) <= 1 or sys.argv[1] == '--help':
    print('')
    print('Usage: ' + sys.argv[0] + ' [-h host[:port]] [-u url] [-f[ramed]] [-s[sl]] [-novalidate] [-ca_certs certs] [-keyfile keyfile] [-certfile certfile] function [arg1 [arg2...]]')
    print('')
    print('Functions:')
    print('  TReturnCode set_sampler(string sampler_name)')
    print('  string get_sampler()')
    print('  TReturnCode set_sampler_value(string key, double value)')
    print('  string get_sampler_value(string key)')
    print('  bool is_measuring()')
    print('  TReturnCode measure(TSamplingConfig config)')
    print('  TResult to_query()')
    print('')
    sys.exit(0)

pp = pprint.PrettyPrinter(indent=2)
host = 'localhost'
port = 9090
uri = ''
framed = False
ssl = False
validate = True
ca_certs = None
keyfile = None
certfile = None
http = False
argi = 1

if sys.argv[argi] == '-h':
    parts = sys.argv[argi + 1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    argi += 2

if sys.argv[argi] == '-u':
    url = urlparse(sys.argv[argi + 1])
    parts = url[1].split(':')
    host = parts[0]
    if len(parts) > 1:
        port = int(parts[1])
    else:
        port = 80
    uri = url[2]
    if url[4]:
        uri += '?%s' % url[4]
    http = True
    argi += 2

if sys.argv[argi] == '-f' or sys.argv[argi] == '-framed':
    framed = True
    argi += 1

if sys.argv[argi] == '-s' or sys.argv[argi] == '-ssl':
    ssl = True
    argi += 1

if sys.argv[argi] == '-novalidate':
    validate = False
    argi += 1

if sys.argv[argi] == '-ca_certs':
    ca_certs = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-keyfile':
    keyfile = sys.argv[argi+1]
    argi += 2

if sys.argv[argi] == '-certfile':
    certfile = sys.argv[argi+1]
    argi += 2

cmd = sys.argv[argi]
args = sys.argv[argi + 1:]

if http:
    transport = THttpClient.THttpClient(host, port, uri)
else:
    if ssl:
        socket = TSSLSocket.TSSLSocket(host, port, validate=validate, ca_certs=ca_certs, keyfile=keyfile, certfile=certfile)
    else:
        socket = TSocket.TSocket(host, port)
    if framed:
        transport = TTransport.TFramedTransport(socket)
    else:
        transport = TTransport.TBufferedTransport(socket)
protocol = TBinaryProtocol(transport)
client = TCarrier.Client(protocol)
transport.open()

if cmd == 'set_sampler':
    if len(args) != 1:
        print('set_sampler requires 1 args')
        sys.exit(1)
    pp.pprint(client.set_sampler(args[0],))

elif cmd == 'get_sampler':
    if len(args) != 0:
        print('get_sampler requires 0 args')
        sys.exit(1)
    pp.pprint(client.get_sampler())

elif cmd == 'set_sampler_value':
    if len(args) != 2:
        print('set_sampler_value requires 2 args')
        sys.exit(1)
    pp.pprint(client.set_sampler_value(args[0], eval(args[1]),))

elif cmd == 'get_sampler_value':
    if len(args) != 1:
        print('get_sampler_value requires 1 args')
        sys.exit(1)
    pp.pprint(client.get_sampler_value(args[0],))

elif cmd == 'is_measuring':
    if len(args) != 0:
        print('is_measuring requires 0 args')
        sys.exit(1)
    pp.pprint(client.is_measuring())

elif cmd == 'measure':
    if len(args) != 1:
        print('measure requires 1 args')
        sys.exit(1)
    pp.pprint(client.measure(eval(args[0]),))

elif cmd == 'to_query':
    if len(args) != 0:
        print('to_query requires 0 args')
        sys.exit(1)
    pp.pprint(client.to_query())

else:
    print('Unrecognized method %s' % cmd)
    sys.exit(1)

transport.close()
